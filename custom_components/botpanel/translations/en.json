{
  "config": {
    "step": {
      "user": {
        "title": "BotPanel",
        "description": "Connect Home Assistant to your BotPanel. The API key is in BotPanel → Settings → API / Integration.",
        "data": {
          "base_url": "BotPanel URL (e.g. http://192.168.1.50:8080)",
          "api_key": "API key"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect: check the BotPanel URL.",
      "invalid_auth": "Invalid API key."
    },
    "abort": {
      "already_configured": "BotPanel is already configured."
    }
  }
}
